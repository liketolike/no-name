#include<stdio.h>

double FindAvarage(double a[10])
{
	int b;
	double avarage;
	double sum = FindAvarage[0];

	for (b = 0; b < 10; b += 1)
	{
		sum += FindAvarage[b];
	}
	avarage = sum / 10;
	return avarage;

}

int main(void)
{
	




















	return 0;
}